version: 2

sources:
  - name: stripe
    database: workspace
    schema: dbt_besiibishi
    tables:
      - name: stripe_payment
        loaded_at_field: _batched_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}